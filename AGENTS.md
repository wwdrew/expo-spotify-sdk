# AGENTS.md

Guidance for AI coding agents working in this repo.

## Agent skills

### Issue tracker

GitHub Issues on `wwdrew/expo-spotify-sdk` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root (created lazily as terms and decisions crystallise). See `docs/agents/domain.md`.
